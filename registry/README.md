# Registry

Fallback `easy-config-schema.json` templates for skills that do not yet ship their own protocol file.

- Prefer skill-local schema when you control the skill repo.
- Remove registry entries once the skill includes its own schema.

P1 will add example entries (e.g. `aaron-web-search.easy-config-schema.json`).
