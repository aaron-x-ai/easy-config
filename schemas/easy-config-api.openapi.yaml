openapi: 3.1.0
info:
  title: Easy Config Local API
  version: 0.1.0
paths:
  /health:
    get:
      summary: Liveness probe
      responses:
        "200":
          description: OK
  /config:
    get:
      summary: SPA entry (P0 placeholder)
